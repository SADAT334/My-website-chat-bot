# services/email.py

def send_email_transcript(user_message, bot_response):
    """Optional function to log transcripts or send email notifications."""
    # You can configure SMTP email sending here later if desired.
    pass